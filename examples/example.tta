var a = 5
var b:       float
b = 1.2
var c = "Hello, world!"
var d: int = a
d = 10
e = true
var cond = true
while cond do
    cond = false;
    test

var b: (int -> int)

func foo: (-> -> int)
    func: (-> int) -> 5
var foo = func: (-> -> int) -> func -> func -> 5

var a = if b then (if c then "foo" else "bar") else "baz"

var block = {
    foo
    bar
    baz
}

match foo
    case 0 -> zero()
    case (_, :int) -> one()
    case x: double
        two()
        x
    